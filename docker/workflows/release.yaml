name: BeiDou Release Workflow

on:
  workflow_dispatch:
    inputs:
      release_version:
        description: "no prefix release version (e.g. 1.12)"
        required: true
        type: string

env:
  GHCR_REGISTRY: ghcr.io
  GHCR_NAMESPACE: beidoums
  DOCKER_REGISTRY: docker.io
  DOCKER_NAMESPACE: ${{ secrets.DOCKER_USER }}
  RELEASE_IMAGE: beidou-server-all

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  Backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Log in to Docker Hub
        if: env.DOCKER_NAMESPACE != ''
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASS }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.GHCR_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push with bake
        uses: docker/bake-action@v5
        with:
          workdir: ./docker
          files: docker-bake.hcl
          targets: release
          set: |
            *.cache-from=type=gha,scope=release
            *.cache-to=type=gha,mode=max,scope=release
          push: true
          no-cache: true
        env:
          IMAGE_TAG_RELEASE_GHCR: ${{ env.GHCR_REGISTRY }}/${{ env.GHCR_NAMESPACE }}/${{ env.RELEASE_IMAGE }}:v${{ inputs.release_version }}
          IMAGE_TAG_RELEASE_DOCKER: ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_NAMESPACE }}/${{ env.RELEASE_IMAGE }}:v${{ inputs.release_version }}
          ARG_RELEASE_VERSION: ${{ inputs.release_version }}
